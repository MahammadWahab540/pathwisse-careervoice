*** Begin Patch
*** Update File: server.ts
@@
 import { getSupabase, requireSupabase } from './src/lib/supabase';
+import { registerInstitutionRoutes } from './src/server/institutionRoutes';
@@
 const app = express();
 const PORT = Number(process.env.PORT || 5000);
 app.use(express.json({ limit: '10mb' }));
+
+// Mount institution onboarding/placement APIs before Vite/static SPA fallbacks.
+// The router authenticates each protected request with the Supabase bearer token.
+const institutionSupabase = getSupabase();
+if (institutionSupabase) {
+  registerInstitutionRoutes(app, institutionSupabase);
+} else {
+  console.warn('institution_routes_disabled', { reason: 'SUPABASE_NOT_CONFIGURED' });
+}
*** End Patch
